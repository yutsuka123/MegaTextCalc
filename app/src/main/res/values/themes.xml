<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MegaTextCalc" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:windowSplashScreenBackground">@color/white</item>
        <!-- アプリアイコンのみを表示するため、スプラッシュアイコンを設定しない -->
        <!-- <item name="android:windowSplashScreenAnimatedIcon">@drawable/splashscreen</item> -->
        <item name="android:windowSplashScreenIconBackgroundColor">@color/white</item>
    </style>

    <style name="Theme.MegaTextCalc" parent="Base.Theme.MegaTextCalc" />
　
    <!-- コンパクトなダイアログスタイル：余白を縮小し、ボタンをより上部に配置 -->
    <style name="CompactDialog" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:windowMinWidthMajor">80%</item>
        <item name="android:windowMinWidthMinor">80%</item>
        <item name="android:padding">0dp</item>
        <item name="android:layout_margin">0dp</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:windowNoTitle">false</item>
        <item name="android:buttonBarButtonStyle">@style/CompactButtonStyle</item>
    </style>

    <!-- コンパクトなボタンスタイル -->
    <style name="CompactButtonStyle" parent="Widget.AppCompat.Button.ButtonBar.AlertDialog">
        <item name="android:minHeight">36dp</item>
        <item name="android:padding">0dp</item>
        <item name="android:layout_marginTop">0dp</item>
        <item name="android:layout_marginBottom">0dp</item>
    </style>
</resources>

